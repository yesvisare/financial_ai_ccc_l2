# Core dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-dotenv==1.0.0

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.1

# Jupyter
jupyter==1.0.0
ipykernel==6.26.0

# Utilities
python-multipart==0.0.6

# Optional: AI Service Integration (uncomment if needed)
# openai==1.3.5         # For RAG response generation

# Optional: Database (uncomment if using PostgreSQL for audit trail)
# psycopg2-binary==2.9.9  # PostgreSQL adapter

# Optional: Queue Management (uncomment if using Redis)
# redis==5.0.1          # Redis client for queue management
# celery==5.3.4         # Task queue for background jobs

# Optional: Notifications (uncomment if needed)
# sendgrid==6.11.0      # Email notifications
# requests==2.31.0      # For Slack webhooks

# Optional: Monitoring (uncomment if needed)
# prometheus-client==0.19.0  # Metrics collection
