# L3 M8.2: Real-Time Financial Data Enrichment
# Environment Configuration Template
# Copy this file to .env and fill in your credentials

# ============================================================
# Redis Configuration (Optional - for caching)
# ============================================================
# Enable/disable Redis caching
REDIS_ENABLED=false

# Redis connection parameters (if enabled)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Or use a full connection string (overrides above parameters)
# REDIS_URL=redis://:password@host:port/db

# ============================================================
# OpenAI Configuration (Optional - for full RAG features)
# ============================================================
# Enable/disable OpenAI integration
OPENAI_ENABLED=false

# OpenAI API key (required if OPENAI_ENABLED=true)
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# ============================================================
# Pinecone Configuration (Optional - for vector storage)
# ============================================================
# Enable/disable Pinecone vector database
PINECONE_ENABLED=false

# Pinecone credentials (required if PINECONE_ENABLED=true)
# Get your credentials from: https://app.pinecone.io/
PINECONE_API_KEY=your_pinecone_api_key_here
PINECONE_ENVIRONMENT=your-environment-name
PINECONE_INDEX_NAME=financial-rag

# ============================================================
# Application Settings
# ============================================================
# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ============================================================
# Notes
# ============================================================
# - yfinance (Yahoo Finance) is free and requires no API key
# - Redis is optional but recommended for production (60%+ cost savings)
# - OpenAI and Pinecone are optional (only needed for full RAG features)
# - The system works in "offline mode" with all services disabled
#   (slower, no caching, but fully functional for data enrichment)
